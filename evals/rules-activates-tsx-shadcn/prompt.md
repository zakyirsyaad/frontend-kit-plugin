[Konteks: project Next.js App Router dengan Tailwind CSS v4 dan shadcn/ui.
`components.json` ada, `AGENTS.md` memuat section frontend-kit, dan
`src/app/globals.css` mendefinisikan --status-live, --status-warning, --status-error.]

Tambahkan komponen `ServiceRow` di `src/components/service-row.tsx` yang menampilkan
nama service dan status (live, degraded, atau down).
