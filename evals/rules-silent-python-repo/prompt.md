[Konteks: repositori Python murni. Tidak ada package.json, tidak ada components.json,
tidak ada file .tsx.]

Buat fungsi `parse_status(line: str)` di `app/status.py` yang mengembalikan salah satu
dari "live", "degraded", atau "down".
